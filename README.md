# dot

Neovim config (LazyVim) in `config/nvim`.

## Requirements
Neovim ≥ 0.10, git (recommended: ripgrep, fd, Nerd Font)

## Install
```bash
git clone https://github.com/stasdevxx/dot ~/dot
mkdir -p ~/.config && ln -s ~/dot/config/nvim ~/.config/nvim
nvim  # first run installs plugins
